# Agent Instructions & Constraints

このプロジェクトは、AIエージェントと人間（まんじゅん）が協働して開発するマイクロブログサービスです。

## 核心的原則：コミュニティへの敬意と持続可能性

* **外部リポジトリの保護**: プロジェクトで利用しているフレームワーク（Flask, Svelte等）やライブラリ自体のソースコードを改変しないこと。
* **上流への配慮**: ライブラリ側に修正が必要だと判断した場合は、必ずユーザーに報告し、議論すること。AIが勝手にパッチを作成し、外部へ申請するような振る舞いは厳禁とする。パッチ提案の責任と判断は人間が負う。

### Karpathy Guidelines より

この章のガイドラインは実装速度よりも保守・拡張容易性を重んじています。些細なタスクに関してはあなたが判断してください。

* 推論での実装は行わない事。前提が不足しているのであれば確認する事
* 最小限のコードで解決する事。『YAGNI』
* 外科手術の如く対応してください。関係ない場所は触れない事
* 検証可能なゴールを設定する事。そのためにもテストコードから先に書く事

## 設計・実装方針

* **ドメイン駆動設計 (DDD) の尊重**: 
    - 技術詳細（DBやWebフレームワーク）よりも「ドメイン（投稿、フォロー、ActivityPubの配送等）」の整合性を優先する。
    - クリーンアーキテクチャまで厳格にせずとも、ビジネスロジックがフレームワークに密結合しないよう配慮すること。
* **プロトコル**: 将来的な **ActivityPub** への対応を視野に入れ、ActorやActivity（Create, Follow等）の概念をデータ構造に含めること。
* **可視化**: 設計の意図を伝える際は、`docs/` 配下に Mermaid 形式で図解（クラス図、シーケンス図等）を出力すること。拡張子は `.mmd` とする。
    - 文書として記す場合は `.md` とする

### ドメイン駆動設計参考資料

こちらにユーザーが『オニオン・アーキテクチャ』に基づくディレクトリ毎に梱包するのが望ましいと思い抱くレイヤーを明記する：

* back/
    - ドメイン層
        * Entities
        * Repositories
        * Value-Objects
    - ユースケース（アプリケーション）層
        * UseCase
        * Aggregates
        * Factories
* front/
    - プレゼンテーション（ユーザー・インターフェイス）層
* arthfatcs/
    - インフラストラクチャ層
        * 構成管理ツール向けの構成ファイル
        * RDBテーブル構成生成の為のSQLスクリプト

## 開発サイクルとテスト

* **テスト駆動の推奨**: 
    - 「テストの実行方法」は `README.md` または `Makefile` に明記する。
    - AIがコードを生成する際は、必ず対応するテストコード（標準モジュール『unittest』, 『unittest.mock』 等）をセットで提案すること。
    - リリース可能な状態を維持するため、既存のテストを壊す変更は行わないこと。

### レイヤー毎の作業ブランチの変更

実装やファイル編集を行うに当たって、次を準拠する事：

* ディレクトリ（docs, back, front, artifacts） を横断する変更に関しても、細分してコミットを行うこと。例：
    - `{agent_name}/docs` : ドキュメント原本の編集
    - `{agent_name}/back` : バックエンドのスクリプト編集
    - `{agent_name}/front` : フロントエンドのスクリプト編集
    - `{agent_name}/artifacts` : インフラ構成定義ファイルの編集

### コミットメッセージの接頭辞


生成AIがユーザーの自然言語による指示の上で編集した内容に関しては、コミットメッセージの接頭辞に "[Vibe]" 、続けて編集内容に応じて次のいずれか複数の接頭辞を加え、コミットメッセージに変更内容の概要を記載し、コミットすること：

* "[Add]": コードやファイルの追加
* "[Mod]": 現存コードファイルの内容変更
* "[Del]": 現存コードファイルの削除

## 環境・ツール

* **OS**: Ubuntu
* **Editor**: DOOM Emacs (User is a power user)
* **Stack**: Python (Flask) / JavaScript (Svelte) / SQLiteDocument Title

