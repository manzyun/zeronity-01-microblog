設計に当たっての認識合わせをしたいです。特にどこまでをバックエンド（Flask）で行い、どこまでをフロントエンド（Svelte)で行うのかの切り分けが曖昧です。

さらに、開発者自身はActivityPubの仕様について網羅していないので、いくつかMermaidマークアップの内容に対して加筆修正を加えた上で @docs/ 配下のファイルの内容を改変願います。

なお、作業を行う際は、Gitのブランチを `manzyun/docs` ブランチから `gemini/docs` というブランチを新規チェックアウトし、各見出し毎にコミットを願います。

# fig_srv-crt.mmdの査読修正
このファイルでは、アプリケーションのユーザー含めたサーバー、クライアント、及びデータベースと他ActivityPub実装を行ったサーバーとの交信について、開発者の認識の途中までを記しました。

まずは、このMermaidドキュメントの内容を査読し、シーケンスに誤りや抜けがあれば修正・加筆を願います。

なお、この工程を終えた後のGitへのコミットメッセージは、「[Asrt][Vibe]」という接頭辞を加えて変更内容の概要をコミットメッセージに記してください

# fig_srv-crt.mmdへの『新規ユーザー』のシーケンスの追記
前述の内容を終え次第、新規ユーザー追加の際のシーケンスを `fig_srv-crt.mmd` に書き加えてください。

この工程を終えた際のGitへのコミットメッセージは、「[Add][Vibe]」という接頭辞を加えて、変更内容の概要をコミットメッセージに記してください。

# fig_srv-crt.mmdの内容に基づいたテストコードの内容の改変
以上の内容を終えた後、ここまでの変更内容に基づいて、 @back/tests と @front/tests 内のテストコードの改変・追加を願います

この工程を終えた際oｎGitへのコミットメッセージは、「[Add][Vibe]」という接頭辞を加えて、変更内容の概要をコミットメッセージに記してください。
